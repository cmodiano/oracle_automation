---
# Manage grants for one PDB (or non-CDB root).
# Variables: _db, _pdb_item (key=pdb_name or db_name for non-CDB, value=pdb config)
# Covers: role grants, system privileges, object privileges.
#
# Connection model:
#   CDB    : service_name=_db.db_name  +  session_container=_pdb_item.key
#   non-CDB: service_name=_db.db_name  (session_container omitted)

# --- Baseline PDB Role Grants ---
- name: "Baseline PDB role grants for {{ _bl_pdb_grant_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_grant:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _bl_pdb_grant_user.key }}"
    grants: "{{ _bl_pdb_grant_user.value.grants.roles | default([]) }}"
    grant_mode: "{{ _bl_pdb_grant_user.value.grants.grant_mode | default('append') }}"
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | dict2items }}"
  loop_control:
    loop_var: _bl_pdb_grant_user
    label: "{{ _pdb_item.key }}/{{ _bl_pdb_grant_user.key }}"
  when:
    - ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | length > 0
    - _bl_pdb_grant_user.value.grants is defined
    - _bl_pdb_grant_user.value.grants.roles is defined
    - _bl_pdb_grant_user.value.state | default('present') == 'present'

# --- Baseline PDB System Privileges ---
- name: "Baseline PDB system privs for {{ _bl_pdb_syspriv_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_privs:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _bl_pdb_syspriv_user.key }}"
    privs: "{{ _bl_pdb_syspriv_user.value.grants.system_privileges }}"
    privs_type: system
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | dict2items }}"
  loop_control:
    loop_var: _bl_pdb_syspriv_user
    label: "{{ _pdb_item.key }}/{{ _bl_pdb_syspriv_user.key }}"
  when:
    - ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | length > 0
    - _bl_pdb_syspriv_user.value.grants is defined
    - _bl_pdb_syspriv_user.value.grants.system_privileges is defined
    - _bl_pdb_syspriv_user.value.state | default('present') == 'present'

# --- Baseline PDB Object Privileges ---
- name: "Baseline PDB object privs for {{ _bl_pdb_objpriv_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_privs:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _bl_pdb_objpriv_user.key }}"
    privs: "{{ _bl_pdb_objpriv_user.value.grants.object_privileges }}"
    privs_type: object
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | dict2items }}"
  loop_control:
    loop_var: _bl_pdb_objpriv_user
    label: "{{ _pdb_item.key }}/{{ _bl_pdb_objpriv_user.key }}"
  when:
    - ((oracle_baseline_accounts | default({})).pdb | default({})).users | default({}) | length > 0
    - _bl_pdb_objpriv_user.value.grants is defined
    - _bl_pdb_objpriv_user.value.grants.object_privileges is defined
    - _bl_pdb_objpriv_user.value.state | default('present') == 'present'

# --- Catalog PDB Role Grants ---
- name: "Role grants for {{ _pdb_grant_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_grant:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _pdb_grant_user.key }}"
    grants: "{{ _pdb_grant_user.value.grants.roles | default([]) }}"
    grant_mode: "{{ _pdb_grant_user.value.grants.grant_mode | default('append') }}"
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ _pdb_item.value.users | default({}) | dict2items }}"
  loop_control:
    loop_var: _pdb_grant_user
    label: "{{ _pdb_item.key }}/{{ _pdb_grant_user.key }}"
  when:
    - _pdb_grant_user.value.grants is defined
    - _pdb_grant_user.value.grants.roles is defined
    - _pdb_grant_user.value.state | default('present') == 'present'

# --- Catalog PDB System Privileges ---
- name: "System privs for {{ _pdb_syspriv_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_privs:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _pdb_syspriv_user.key }}"
    privs: "{{ _pdb_syspriv_user.value.grants.system_privileges }}"
    privs_type: system
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ _pdb_item.value.users | default({}) | dict2items }}"
  loop_control:
    loop_var: _pdb_syspriv_user
    label: "{{ _pdb_item.key }}/{{ _pdb_syspriv_user.key }}"
  when:
    - _pdb_syspriv_user.value.grants is defined
    - _pdb_syspriv_user.value.grants.system_privileges is defined
    - _pdb_syspriv_user.value.state | default('present') == 'present'

# --- Catalog PDB Object Privileges ---
- name: "Object privs for {{ _pdb_objpriv_user.key }} in {{ _db.db_name }}/{{ _pdb_item.key }}"
  ibre5041.ansible_oracle_modules.oracle_privs:
    mode: sysdba
    service_name: "{{ _db.db_name }}"
    session_container: "{{ _pdb_item.key if (_db.cdb | default(true)) else omit }}"
    oracle_home: "{{ _db._oracle_home_path }}"
    schema: "{{ _pdb_objpriv_user.key }}"
    privs: "{{ _pdb_objpriv_user.value.grants.object_privileges }}"
    privs_type: object
    state: present
  become_user: "{{ oracle_os_user }}"
  loop: "{{ _pdb_item.value.users | default({}) | dict2items }}"
  loop_control:
    loop_var: _pdb_objpriv_user
    label: "{{ _pdb_item.key }}/{{ _pdb_objpriv_user.key }}"
  when:
    - _pdb_objpriv_user.value.grants is defined
    - _pdb_objpriv_user.value.grants.object_privileges is defined
    - _pdb_objpriv_user.value.state | default('present') == 'present'
